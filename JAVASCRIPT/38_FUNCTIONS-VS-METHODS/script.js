const maths = {
    E: 2.718281828459045,
    add: function(a, b) {
      return a + b
   }, // method
   square: function(num) {
       return num * num // x ** 2
   },
   subtract(a, b) {
       return a - b
   },
   cube(num) {
       return num ** 3
   }
}

// function add(a, b) {
//     return a + b
// } // function declaration